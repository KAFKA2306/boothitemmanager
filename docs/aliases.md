# Aliases

Reference for `aliases.yml` configuration.
Configures character normalization (NFKC, case-insensitive) and maps avatar names/aliases to standardized codes.
Also normalizes item types (e.g., "3D Avatar" -> "avatar").
