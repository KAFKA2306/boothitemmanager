version: '3'

tasks:
  build:
    desc: Build the published dashboard from the checked-in static API
    cmds:
      - bash build_static.sh

  refresh:
    desc: Re-observe a bounded BOOTH slice and update material catalogue changes
    cmds:
      - python3 scripts/refresh_catalog.py --api-dir api --interval-minutes 15
      - python3 scripts/audit_static_api.py api --report build/static-api-refresh-audit.json --max-bytes 4500000

  rebuild-data:
    desc: Rebuild derived data from data/raw/index.ndjson when that source dataset is present
    cmds:
      - test -f data/raw/index.ndjson
      - uv run python3 scripts/run_bulk_pipeline.py

  serve:
    desc: Serve the generated site
    cmds:
      - cd dist && python3 -m http.server 8080
    interactive: true

  mcp-serve:
    desc: Start the Model Context Protocol server for catalogue and SEO metadata
    cmds:
      - uv run python3 scripts/mcp_seo_server.py

  clean:
    desc: Clean build artifacts
    cmds:
      - rm -rf dist build

  clean-cache:
    desc: Clean metadata cache
    cmds:
      - rm -f booth_item_cache.json

  lint:
    desc: Lint code
    cmds:
      - uv run ruff check src

  format:
    desc: Format code
    cmds:
      - uv run ruff format src

  test:
    desc: Run tests
    cmds:
      - uv run pytest

  check:
    desc: Run lint, format, and tests
    deps: [lint, format]
    cmds:
      - task: test
