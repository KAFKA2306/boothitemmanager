# AXIOMATIC SPACE INITIALIZED

この空間は、以下の「公理（Axioms）」によって統治される「Axiomatic Space」です。
すべての成果物は、感情や言葉ではなく、「実証された事実」のみによって評価されます。

## Foundational Axioms (基本公理)

1.  **Zero-Fat (脂肪ゼロ)**:
    - 防御的なボイラープレート、未使用のコード、およびコメントは一切禁止。
    - ロジックは純粋かつ最小限であること。
    - ドキュメントは論理的・目的別に統合し、断片化を許さない。
2.  **Artifact Provenance (成果物の系譜)**:
    - すべてのファイルには検証可能な系譜が必要。
    - 「発見 → ハッシュ検証 → 実行」のシーケンスを遵守せよ。
3.  **Evidence-Based Completion (エビデンスに基づく完了)**:
    - ファイルが存在することは完了の証拠にならない。
    - システムが生成したログと、再現可能なコマンド出力のみを真実として認める。
4.  **Logic/Infra Separation (分離の原則)**:
    - ビジネスロジックにリトライやエラーハンドリングを混在させない。
    - それらは Taskfile や Makefile などのインフラ層に集約せよ。

## Technical Mandates (技術的命令)

-   **Cloudflare Compatibility (プラットフォーム制約)**:
    - 単一アセットの最大サイズは **25 MiB**。超過はデプロイ失敗とみなす。
    - 巨大な JSON は必ずシャーディング（分割）し、並列ロードを実装せよ。
-   **Evolving Ontology (概念の進化)**:
    - オントロジーは静的ではなく、クローラーからのフィードバックにより進化する。
    - 数字ベースの低品質タグ（例: "40アバター対応"）は監査（Audit）で物理排除せよ。
-   **Novelty-Aware Similarity (発見の最大化)**:
    - 類似アイテムは単純な一致度だけでなく、公開日（新しさ）とクリエイターの多様性を加味してスコアリングせよ。

## Development Standard: Crash-Driven (CDD)

-   **Stack Trace as Scripture (聖典としてのスタックトレース)**:
    - クラッシュは情報取得イベント。
    - スタックトレースのみが、次に行うべき正しいアクションを示す。
-   **No try-catch**:
    - ビジネスロジックでエラーを隠蔽するな。
    - 即座にクラッシュさせ、Auditorに正確な状態を報告せよ。

---
**この空間において、言葉による説明（Prose）は無効。検証ログのみを提示せよ。**
